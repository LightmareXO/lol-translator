# Issue #8 進捗メモ

## 現在地

- Issue #3 のJSON契約と正規化座標を再利用している。
- 元動画はYouTube ID `QK95uTvf7ks`、SHA-256はmanifestに記録した。
- Issue #3で指定した縦位置を保ち、1920x1080映像上の字幕帯を `x=38, y=862, width=1844, height=96` とした。最長字幕を保つため横幅だけを広げ、右端のミニマップも一部含む固定範囲である。
- 8種類の字幕について、60fpsの隣接3フレームずつ、計24枚を選定した。
- 正解文は中央フレームと24枚のコンタクトシートを目視して確認した。
- 動画内の字幕はすべて1行だった。2行字幕は別動画を用いた追加検証とし、今回の結果を2行字幕へ一般化しない。

## ローカルデータの再抽出

動画、抽出画像、モデル、仮想環境、結果の一時ファイルはGitへ含めない。

```powershell
python analyzer/ocr_evaluation/extract_frames.py `
  --manifest analyzer/ocr_evaluation/manifest.json `
  --video "<テスト動画の絶対パス>" `
  --output-dir analyzer/ocr_evaluation/work/images
```

抽出時に動画のSHA-256、解像度、manifest、出力の上書きを検証する。

## 完了したこと

1. PaddleOCRとEasyOCRを分離した仮想環境で実画像のCPU推論を確認した。
2. 両方式と4種類の固定前処理を組み合わせ、24枚を逐次実行した。
3. 精度・速度・安定性・ピークメモリ・ディスク使用量を集計した。
4. PaddleOCR＋固定コントラスト補正を1行字幕向けに採用した。
5. 実測結果、代表的な失敗、Issue #7への引き継ぎを `REPORT.md` にまとめた。

## 次に再開する条件

別動画の2行字幕を少なくとも6種類、各3隣接フレーム用意し、まず同じ認識専用構成を追加検証する。
失敗する場合だけ、上下2分割または文字検出ありを別構成として比較する。
