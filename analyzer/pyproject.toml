[project]
name = "lol-translator-analyzer"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
dependencies = []

[tool.lol-translator]
# OCR dependencies will be pinned here after the OCR evaluation.
